# diffpad - accessible via /diffpad
alias.url += ( "/diffpad" => "/var/www/html/diffpad" )

# Optional: Set up directory browsing and index file for the diffpad directory
$HTTP["url"] =~ "^/diffpad($|/)" {
    server.indexfiles = ("diffpad.html")
    
    # Enable serving static files
    static-file.exclude-extensions = (".fcgi", ".php", ".rb", "~", ".inc")
    
    # Optional: Enable compression for this path
    compress.filetype = ("text/plain", "text/html", "text/css", "text/javascript", "application/javascript")
}
